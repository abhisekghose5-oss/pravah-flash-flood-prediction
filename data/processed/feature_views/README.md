# Named feature views

These CSVs are derived from the canonical processed catchment tables. Every view keeps the original cleaned `GaugeID` so it can be joined without changing backend or ML contracts. The inference engine and training artifacts continue to use their existing canonical files.
